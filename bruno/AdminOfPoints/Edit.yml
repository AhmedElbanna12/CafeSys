info:
  name: Edit
  type: http
  seq: 3

http:
  method: PUT
  url: http://biscofa.runasp.net/api/admin/settings/points
  body:
    type: json
    data: "{\r\n  \"egpPerPoint\": 10\r\n}"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
