info:
  name: Get
  type: http
  seq: 1

http:
  method: GET
  url: http://biscofa.runasp.net/api/admin/settings/points
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
