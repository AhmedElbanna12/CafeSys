info:
  name: Get
  type: http
  seq: 3

http:
  method: GET
  url: http://biscofa.runasp.net/api/admin/modifier-groups/154/options?lang=en
  params:
    - name: lang
      value: en
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
